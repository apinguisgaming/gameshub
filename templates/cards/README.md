# Handcrafted Game Card Partials
Place optional handcrafted card templates here named `<game_id>.html` (e.g. `secret.html`, `song.html`).
If present, GameHub will include the handcrafted card instead of the auto-generated card.
